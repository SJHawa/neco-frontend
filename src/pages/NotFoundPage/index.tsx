import { PageShell } from "../../shared/components/PageShell";

export function NotFoundPage() {
  return (
    <PageShell
      title="Not Found"
      description="The requested route does not exist in the current application shell."
    />
  );
}
